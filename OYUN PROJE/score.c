#include <stdio.h>
#include <stdlib.h>

int SCORE = 0;

void setFeatureValue(int *feature, int value) {
    if (value > MAX_FEATURE_VALUE) {
        *feature = MAX_FEATURE_VALUE;
    }
    else {
        *feature = value;
    }
void score()
}
